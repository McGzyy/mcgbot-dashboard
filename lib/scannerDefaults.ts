/** Defaults when the bot API cannot serve scanner settings (matches bot preset ladder intent). */
export const DEFAULT_APPROVAL_TRIGGER_X = 4;
export const DEFAULT_APPROVAL_MILESTONE_LADDER = [2, 3, 5, 8, 12, 20, 30, 50, 74, 100];
